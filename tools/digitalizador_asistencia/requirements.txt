# Núcleo (obligatorio)
numpy>=1.24
opencv-python-headless>=4.8
pypdfium2>=4.0          # lectura de PDF escaneados (alternativa: pymupdf)

# --- Opcionales, según lo que uses -----------------------------------------
# Motor por defecto (claude-hoja / claude): API de visión de Claude
# anthropic>=1.0

# Motor local sin API (trocr)
# transformers>=4.40
# torch>=2.0
# pillow>=10.0

# Motor offline para letra de imprenta (tesseract)
# pytesseract>=0.3.10      # requiere el binario: apt-get install tesseract-ocr tesseract-ocr-spa

# Cotejo con el padrón más rápido y preciso (si falta, se usa difflib)
# rapidfuzz>=3.0

# Salida a Excel
# openpyxl>=3.1

# Pruebas (pillow genera los PDF sintéticos)
# pytest>=7.0
# pillow>=10.0
